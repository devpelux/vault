<fc:EWindow x:Class="Vault.SettingsWindow"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:fc="http://devpelux.github.io/fullcontrols"
            mc:Ignorable="d"
            Title="Impostazioni"
            Height="400"
            Width="600"
            WindowStyle="None"
            CornerRadius="{DynamicResource WindowCornerRadius}"
            BorderThickness="1"
            BorderBrush="{DynamicResource WindowBorderBrush}"
            Foreground="White"
            CaptionButtonsForeground="White"
            CaptionButtonsForegroundOnMouseOver="White"
            CaptionButtonsForegroundOnPressed="White"
            Background="{DynamicResource WindowBackground}"
            CaptionButtonsBackground="{DynamicResource WindowBackground}"
            CaptionButtonsBackgroundOnMouseOver="#FF575759"
            CaptionButtonsBackgroundOnPressed="#FF2D2D30"
            ToolbarBackground="{DynamicResource WindowBackground}"
            Icon="/Icons/VaultIcon.ico"
            WindowStartupLocation="CenterScreen"
            ResizeMode="NoResize"
            Closed="Window_Closed"
            Loaded="Window_Loaded">
    <Grid>
        <fc:ECheckBox x:Name="AutoStart"
                      Style="{DynamicResource ECheckBoxDark}"
                      HorizontalAlignment="Left"
                      VerticalAlignment="Top"
                      Margin="35,40,0,0"
                      Height="15"
                      Content="Avvia all'avvio di windows."
                      Checked="AutoStart_Checked"
                      Unchecked="AutoStart_Unchecked"/>
        <fc:ECheckBox x:Name="StartHided"
                      Style="{DynamicResource ECheckBoxDark}"
                      HorizontalAlignment="Left"
                      VerticalAlignment="Top"
                      Margin="35,70,0,0"
                      Height="15"
                      Content="Avvia nascosto nella tray."
                      Checked="StartHided_Checked"
                      Unchecked="StartHided_Unchecked"/>
        <fc:ECheckBox x:Name="RememberDBPassword"
                      Style="{DynamicResource ECheckBoxDark}"
                      HorizontalAlignment="Left"
                      VerticalAlignment="Top"
                      Margin="35,120,0,0"
                      Height="15"
                      Content="Ricorda la password del database."
                      Unchecked="RememberDBPassword_Unchecked"/>
    </Grid>
</fc:EWindow>
